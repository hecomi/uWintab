#pragma once

#include <Windows.h>
#include <thread>


/*
class Window
{
public:
    void Initialize();
    void WndProc(HWND hWnd, UINT msg, WPARAM wp, LPARAM lp);

private:
    void OpenWindow();
    void CloseWindow();
    void WndProcForWindow(HWND hWnd, UINT msg, WPARAM wp, LPARAM lp);
    void WndProcForWintab(HWND hWnd, UINT msg, WPARAM wp, LPARAM lp);

    HWND hWnd_;
    std::thread thread_;
};
*/